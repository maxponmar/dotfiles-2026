# Keep LF line endings everywhere so shell scripts and the Python statusline
# work after a clone on Windows (CRLF breaks shebangs and `source`).
* text=auto eol=lf
*.sh   text eol=lf
*.zsh  text eol=lf
*.py   text eol=lf
*.lua  text eol=lf
*.conf text eol=lf
*.toml text eol=lf
*.ps1  text eol=lf
